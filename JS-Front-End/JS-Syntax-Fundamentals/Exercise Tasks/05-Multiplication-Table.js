function printTimesTable(number) {
    for (let i = 1; i <= 10; i++) {
        console.log(`${number} X ${i} = ${number * i}`);
    }
}

// Test cases
printTimesTable(5); // Output: see example in the question
printTimesTable(2); // Output: see example in the question